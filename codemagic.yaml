Workflows:
  deploy:
    name: Deploy Workflow
    # الحفاظ على الحد الأقصى لمدة البناء 120 دقيقة 
    # (وهو الحد الأقصى المسموح به في Codemagic).
    max_build_duration: 120
    
    triggering:
      events:
        - push
        - pull_request
      branch_patterns:
        - pattern: master
          include: true
          
      # 🆕 إضافة قسم الجدولة لإعادة تشغيل البناء كل ساعتين
      scheduling:
        - name: Periodic Deployment Restart
          # تحديد الفرع المراد بناؤه
          branch: master 
          # تعبير Cron لتشغيل البناء في الدقيقة 0 من كل ساعتين (* /2)
          cron: "0 */2 * * *" 

    environment:
      node: "20"

    scripts:
      - name: Install Dependencies
        script: npm install

      - name: Start Project
        # ⚠️ ملاحظة: هذا الأمر (npm start) سيعمل في Codemagic 
        # حتى يصل البناء إلى المهلة القصوى (120 دقيقة).
        script: npm start

    artifacts:
      - "**/*.log"
