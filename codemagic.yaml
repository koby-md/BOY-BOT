workflows:
  deploy:
    name: Deploy Workflow

    # 🛑 تم إزالة schedules (يجب إعداد الجدولة في واجهة مستخدم Codemagic أو البحث عن طريقة التضمين الصحيحة)
    # 🛑 تم إزالة meta (يجب البحث عن الحقل الصحيح لـ abort_previous_builds)

    # 💡 يتم تفعيل هذا السير العمل افتراضيًا عند الدفع والسحب إلى الفروع
    # 💡 يمكن حذف قسم triggering إذا كنت تعتمد على الإعدادات الافتراضية، أو يجب التحقق من الموقع الصحيح له.

    environment:
      node: "20"

    scripts:
      - name: Install Dependencies
        script: npm install

      - name: Start Project
        script: npm start

    artifacts:
      - "**/artifacts.log" # لاحظ: قمت بتغيير **/*.log إلى ** /artifacts.log كونه شائع

    # يمكن إضافة هذا إذا كان مدعومًا باسم حقل صحيح
    # auto_cancel_builds: true
